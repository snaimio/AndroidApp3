<?xml version="1.0" encoding="utf-8"?>
<!--
    ============================================================
    item_tool.xml - Tool Card Layout for RecyclerView
    ============================================================
    FIXED: Touch target size is now 48dp (minimum recommended)
    - Button height: 48dp (was 44dp)
    - Button minWidth: 100dp (kept)
    - No more "Touch target too small" warning
-->

<androidx.cardview.widget.CardView
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/toolCard"
    android:layout_width="match_parent"
    android:layout_height="200dp"
    app:cardCornerRadius="16dp"
    app:cardElevation="4dp"
    android:clickable="true"
    android:focusable="true"
    android:foreground="?android:attr/selectableItemBackground"
    android:layout_margin="8dp"
    app:cardBackgroundColor="#2A2A4E">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:orientation="vertical"
        android:padding="16dp"
        android:gravity="center">

        <!-- TOOL ICON -->
        <TextView
            android:id="@+id/toolIcon"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:textSize="40sp"
            android:layout_gravity="center" />

        <!-- TOOL TITLE -->
        <TextView
            android:id="@+id/toolTitle"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:textSize="16sp"
            android:textStyle="bold"
            android:textColor="#FFFFFF"
            android:layout_gravity="center"
            android:layout_marginTop="4dp"
            android:maxLines="1"
            android:ellipsize="end" />

        <!-- TOOL DESCRIPTION -->
        <TextView
            android:id="@+id/toolDesc"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:textSize="13sp"
            android:textColor="#888888"
            android:gravity="center"
            android:layout_gravity="center"
            android:layout_marginTop="2dp"
            android:maxLines="2"
            android:ellipsize="end" />

        <!-- ============================================================
             TOOL BUTTON - FIXED: Touch target is now 48dp
             ============================================================
             FIXES:
             - height: 48dp (minimum recommended touch target)
             - minWidth: 100dp (comfortable to tap)
             - padding: 12dp (space around text)
             - No more "Touch target too small" warning
        -->
        <Button
            android:id="@+id/toolButton"
            android:layout_width="wrap_content"
            android:layout_height="48dp"
            android:minWidth="100dp"
            android:text="@string/button_open"
            android:textSize="14sp"
            android:textColor="#FFFFFF"
            android:singleLine="true"
            android:gravity="center"
            android:layout_gravity="center"
            android:layout_marginTop="8dp"
            android:paddingStart="20dp"
            android:paddingEnd="20dp"
            android:paddingTop="8dp"
            android:paddingBottom="8dp"
            android:background="@drawable/button_primary" />

    </LinearLayout>

</androidx.cardview.widget.CardView>